# @package _global_

init_dict:
  model_type_or_dir: lamdo/distilbert-s2orc-mlm-80000steps
  model_type_or_dir_q: null
  freeze_d_model: 0
  agg: max
  fp16: true

config:
  tokenizer_type: lamdo/distilbert-s2orc-mlm-80000steps
  matching_type: splade

